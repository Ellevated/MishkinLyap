/**
 * Module: main
 * Role: Entry point — SDK init → Phaser boot
 * Uses: sdk/IGamePlatform, scenes/*, config/GameConfig
 * Used by: index.html (script tag)
 * Does NOT: contain game logic
 */

// Placeholder — FTR-002 will implement SDK bridge + Phaser boot
console.log('Zverata: main.ts loaded');
